ALX TypeScript Project

This project introduces TypeScript fundamentals, interfaces, and Webpack bundling. The objective is to write strongly-typed code, compile it, and ensure no TypeScript errors occur during build.

✅ Requirements

Allowed editors: vi, vim, emacs, Visual Studio Code

All files must end with a newline

All files will be transpiled on Ubuntu 18.04

Code will be checked using Jest (v24.9.*)

README.md file is mandatory

TypeScript code must use .ts extension

The TypeScript compiler must not show any errors

Webpack is used to bundle the output into dist/

📂 Project Structure
task_0/
 ├── dist/                # Webpack build output
 ├── main.ts              # TypeScript source file
 ├── package.json
 ├── tsconfig.json
 ├── webpack.config.js
 ├── .eslintrc.js
 └── index.html           # Used to render table in browser

🧠 Learning Goals

Understand and declare TypeScript interfaces

Work with typed objects & arrays

Compile TypeScript using tsc

Bundle and serve files using Webpack

Use DOM APIs in TypeScript

Follow ES6/TypeScript best practices

🚀 Task Summary
Task 0 — Student Interface & Table Rendering

Create a Student interface

Create two student objects

Store them in an array

Render student names & locations in an HTML table using Vanilla JS & TS

📦 Install Dependencies
npm install

🔧 Build the Project
npm run build


You should see:

No type errors found.
Compiled successfully.
